name: Create Copy Action PR

on:
  push: 
    branches: 
        - 'copy-from-api-ref'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create PR
        env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
            gh pr create --title "Copy changelog from API Ref" --head "copy-from-api-ref" --base main
